{
    "layer": "top",
    "position": "bottom",

    "height": 25,

    "modules-left": [
        "hyprland/workspaces"
    ],
    "modules-center": [

    ],
    "modules-right": [
        "network",
        "battery",
        "tray",
        "clock#date",
        "clock#time"
    ],

    "network": {
        "interval": 5,
        "format-wifi": "{essid}",
        "format-ethernet": "{ifname}: {ipaddr}/{cidr}",
        "format-disconnected": "(disconnected)",
        "tooltip-format": "{ifname}: {ipaddr}"
    },

    "clock#date": {
      "format": "{:%Y-%m-%d}",
    }
}
