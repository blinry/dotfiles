[core]
    excludesfile = ~/.gitignore_global
	autocrlf = input
[color]
    ui = true
[alias]
    ci = commit
    co = checkout
    st = status
    df = diff
    graph = log --graph --pretty=oneline --abbrev-commit --all --decorate
    take = checkout -b
[push]
	default = current
